includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6

    paths:
        - app
        - routes
        - tests

    # Blade compiles to PHP in here; it is generated, not written.
    excludePaths:
        - storage
        - bootstrap/cache
